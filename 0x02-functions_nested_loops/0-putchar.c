#include <stdio.h>

/**
 * main - Entry Point
 * Description - program that prints _putchar
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char k;

	for (k = 'A'; k <= 'Z'; k++)
	{
		putchar(k);
	}

	return (0);
}
